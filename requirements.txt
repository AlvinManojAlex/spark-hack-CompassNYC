# Compass NYC — Python Dependencies
# Install with: pip install -r requirements.txt

# Core ML
numpy>=1.24.0
sentence-transformers>=2.2.0

# API calls
requests>=2.28.0

# Optional but recommended for production
# pandas>=2.0.0  # If you want to use RAPIDS-style data processing later
# torch>=2.0.0   # Will be auto-installed by sentence-transformers, but explicit is better
